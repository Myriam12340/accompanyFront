import { EntretientViewModel } from './entretient-view-model';

describe('EntretientViewModel', () => {
  it('should create an instance', () => {
    expect(new EntretientViewModel()).toBeTruthy();
  });
});
